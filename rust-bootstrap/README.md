# Rust bootstrap

Rust application template using the 2024 edition. `rust-toolchain.toml` fixes
the compiler and required components for consistent local and CI checks.

```bash
make check
cargo run
```
