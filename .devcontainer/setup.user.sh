#!/usr/bin/env -S bash -euo pipefail

echo "Setting up user environment..."
# mkdir -p ~/.local/bin

# if [ ! -f ~/.local/bin/starship ]; then
#   curl -fsSL \
#     https://github.com/starship/starship/releases/download/v1.26.0/starship-$(uname -m)-unknown-linux-musl.tar.gz \
#     | tar xz -C ~/.local/bin
#   echo 'eval "$(~/.local/bin/starship init bash)"' >> ~/.bashrc
# fi

# sudo apt update -qq
# sudo apt install -qq -y vim

# uv sync

# echo 'export PATH="/workspaces/template-devcontainer/.venv/bin:$PATH"' >> ~/.bashrc
# "$(command -v code)" --install-extension {extension-id}
